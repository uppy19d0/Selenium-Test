# URL base del entorno a probar
base_url: https://plataformavirtual.itla.edu.do

#chrome/firefox/edge
browser: firefox

# True/False (si es true no abre ventana, ideal para CI)
headless: false

# Timeout en segundos para esperas explícitas
wait_timeout: 10
